const executeCode = async ({
  code,
  language,
  input,
}) => {
  // Execution engine will be
  // integrated here

  return {
    output: "",
    error: null,
    executionTime: 0,
    status: "SUCCESS",
  };
};

export {
  executeCode,
};